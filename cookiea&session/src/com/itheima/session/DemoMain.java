package com.itheima.session;

/**
 * httpSession：服务端的会话管理技术，用于存储用户会话数据
 *    获取session对象
 *      getSession()获取session对象
 *      getSession(Boolean b)获取session对象，没有自动创建一个session对象
 *   方法：
 *      setAttribute(String name,Object o)设置共享域对象
 *      getAttribute(String name)获取共享域对象
 *      removeAttribute(String name)移除共享域对象
 *      getId()获取唯一标识名称JSession值
 *      invalidate()设置共享域立即失效
 *
 */
public class DemoMain {

}
